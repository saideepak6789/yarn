steps:
  - name: "gcr.io/cloud-builders/gsutil"
    args:
      [
        "-m",
        "rsync",
        "-r",
        "-x",
        ".git/",
        "-c",
        "./style",
        "gs://${_GCS_BUCKET}",
      ]
timeout: 3000s
options:
  logging: CLOUD_LOGGING_ONLY
